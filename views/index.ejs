<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>D-ANON</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f9;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
        }

        h1 {
            font-size: 24px;
            color: #333;
            text-align: center;
            margin-bottom: 20px;
        }

        .container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            max-width: 900px;
            margin: 40px 20px;
            width: 100%;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            padding: 12px 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #333;
            color: white;
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        /* Estilos de Seções */
        .section-title {
            background-color: #007BFF;
            color: white;
            padding: 8px;
            font-size: 16px;
            font-weight: bold;
            text-align: center;
            text-transform: uppercase;
            margin: 10px 0;
            border-radius: 5px;
        }

    </style>
</head>
<body>

    <div class="container">
        <h1>Fingerprint Data</h1>

        <!-- Exibir a mensagem no topo da página -->
        <div id="message" style="color: red; font-weight: bold; text-align: center; margin-bottom: 20px;">
            <!-- Aguardando resposta do servidor  -->
        </div>

        <table>
            <thead>
                <tr>
                    <th>Fingerprint</th>
                    <th>Valor</th>
                </tr>
            </thead>
            <tbody>
                <!-- Seção Navegador e Sistema -->
                <tr>
                    <td class="section-title" colspan="2">Navegador e Sistema</td>
                </tr>
                <tbody id="systemData"></tbody>

                <!-- Seção TCP Fingerprints -->
                <tr>
                    <td class="section-title" colspan="2">TCP Fingerprints</td>
                </tr>
                <tbody id="tcpData"></tbody>

                <!-- Seção ClientJS Fingerprints -->
                <tr>
                    <td class="section-title" colspan="2">ClientJS Fingerprints</td>
                </tr>
                <tbody id="clientData"></tbody>

                <!-- Seção Thumbmark Fingerprints -->
                <tr>
                    <td class="section-title" colspan="2">ThumbmarkJS Fingerprints</td>
                </tr>
                <tbody id="thumbmarkData"></tbody>

                <!-- Seção Fingerprints Adicionais -->
                <tr>
                    <td class="section-title" colspan="2">Fingerprints Adicionais</td>
                </tr>
                <tbody id="additionalData"></tbody>
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/clientjs@0.1.11/dist/client.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@thumbmarkjs/thumbmarkjs/dist/thumbmark.umd.js"></script>
    <script type="module" src="/js/mainFingerprint.js"></script>
</body>
</html>
